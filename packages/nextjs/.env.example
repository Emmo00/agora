# Template for NextJS environment variables.
#
# For local development:
# 1. Copy this file: cp .env.example .env.local
# 2. Fill in the values below
#
# For production deployment (Vercel, etc.):
# Set these environment variables in your deployment platform's config
#
# Notes:
# - Variables prefixed with NEXT_PUBLIC_ are accessible on the client side
# - Variables without NEXT_PUBLIC_ are server-only (secure)
# - More info: https://nextjs.org/docs/pages/building-your-application/configuring/environment-variables

# ============================================================================
# BLOCKCHAIN & WALLET
# ============================================================================

# Alchemy API Key (for blockchain RPC)
# Get from: https://www.alchemy.com/
# Optional: Agora provides a default key in scaffold.config.ts if not set
NEXT_PUBLIC_ALCHEMY_API_KEY=

# WalletConnect Project ID (for wallet connection)
# Get from: https://cloud.walletconnect.com/
# Optional: Agora provides a default ID in scaffold.config.ts if not set
NEXT_PUBLIC_WALLET_CONNECT_PROJECT_ID=

# ============================================================================
# IPFS & PINATA
# ============================================================================

# Pinata JWT Token (BACKEND ONLY - KEEP SECRET!)
# This is used by your backend API routes for secure IPFS uploads
# Get from: https://app.pinata.cloud/ > API Keys > Copy JWT
# IMPORTANT: This should NEVER be exposed to the frontend
# Store securely in your deployment platform's secret manager
PINATA_JWT=

# Pinata Gateway Domain (PUBLIC - safe for frontend)
# This is used by the frontend to fetch IPFS content
# Get from: https://app.pinata.cloud/ > Gateways > Copy your gateway domain
# Format: example-llama-123.mypinata.cloud
# If not set, the app will fallback to ipfs.io (slower, may have rate limits)
NEXT_PUBLIC_PINATA_GATEWAY=
